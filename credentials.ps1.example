# PPPoE Credentials Configuration
# Copy this file to 'credentials.ps1' and fill in your actual credentials
# The 'credentials.ps1' file is excluded from Git to prevent accidental commits

# Your ISP PPPoE credentials
# If you leave these empty or null, the script will fall back to saved Windows credentials
$PPPoE_Username = 'your_username@isp.com'
$PPPoE_Password = 'your_password_here'

# Connection name (should match your Windows PPPoE connection)
# If left empty, the script will use the default connection name
$PPPoE_ConnectionName = 'Rise PPPoE'

# Note: Variables are automatically available when this file is dot-sourced
